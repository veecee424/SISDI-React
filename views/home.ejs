<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', { title: 'Football in schools, for holistic child development.' }) %>
  <body>

    <!-- Navbar -->
    <%- include('partials/navbar') %>

    <main>
              <!-- HERO -->
        <%- include('partials/hero', { fadeClass: 'fade-in' }) %>

        <!-- WHY IT MATTERS -->
        <%- include('partials/why-it-matters', { fadeClass: 'fade-in' }) %>
        <div class="section-separator">
          <svg viewBox="0 0 1440 100" preserveAspectRatio="none">
            <path
              d="M0,40 C120,80 320,0 520,20 720,40 920,80 1120,60 1320,40 1440,20 1440,20 L1440,0 L0,0 Z"
            ></path>
          </svg>
        </div>


        <!-- OUR APPROACH -->
        <%- include('partials/our-approach', { fadeClass: 'fade-in' }) %>

        <!-- PAN-AFRICAN IMPACT -->
        <%- include('partials/impact', { fadeClass: 'fade-in' }) %>

        <!-- TESTIMONIALS -->
        <%- include('partials/testimonial', { fadeClass: 'fade-in' }) %>
        <div class="gradient-separator dark"></div>

        <!-- GET INVOLVED -->
        <%- include('partials/get-involved', { fadeClass: 'fade-in' }) %>
      <div class="gradient-separator dark"></div>

        <!-- FINAL CTA -->
        <%- include('partials/cta', { fadeClass: 'fade-in' }) %>
    </main>

    <!-- Footer -->
    <%- include('partials/footer') %>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/scroll-animations.js"></script>
  </body>
</html>

